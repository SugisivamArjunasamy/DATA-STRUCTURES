#include "binary.h"

// Postorder Operation

void postorder(struct node * root){
  // Fill Your Code
  if(root==NULL)
      return;
  postorder(root->left);
  postorder(root->right);
  printf("%d ",root->data);
}
